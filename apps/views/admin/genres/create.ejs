<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <%= typeof title !== 'undefined' ? title : 'Thêm Thể Loại' %> - Admin
    </title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <link rel="stylesheet" href="/style.css" />
    <style>
      .content {
        padding: 20px;
        max-width: 600px;
        margin: auto;
      }
    </style>
  </head>
  <body>
    <%- include('../../partial/menu'); %>

    <main class="container content">
      <%- include('../../partial/flash'); %>

      <h1 class="mb-4">
        <%= typeof title !== 'undefined' ? title : 'Thêm Thể Loại Mới' %>
      </h1>

      <form action="/admin/genres" method="POST">
        <div class="mb-3">
          <label for="genreName" class="form-label"
            >Tên Thể Loại <span class="text-danger">*</span></label
          >
          <input
            type="text"
            class="form-control"
            id="genreName"
            name="name"
            required
            autofocus
          />
          <div class="form-text">
            Nhập tên thể loại muốn thêm (ví dụ: Action, Romance, Isekai...).
          </div>
        </div>

        <div class="mt-4">
          <button type="submit" class="btn btn-primary">Thêm Thể Loại</button>
          <a href="/admin/genres" class="btn btn-secondary">Hủy bỏ</a>
        </div>
      </form>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/script.js"></script>
  </body>
</html>
